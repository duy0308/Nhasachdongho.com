@model Models.EF.M1Footer
@{
    ViewBag.Title = "Footer";
}
@using (Html.BeginForm())
{
    <div class="container" style="margin-top:20px">
        <div><h3>THÔNG TIN FOOTER</h3></div>
        <hr />
        <div class="col-md-12">
            @Html.HiddenFor(m=>m.ID)
            @Html.HiddenFor(m=>m.Status)

            @using (Html.BeginForm())
            {
                @Html.TextAreaFor(model => model.Detail)
                <script type="text/javascript">CKEDITOR.replace('Detail'); </script>
                @*@Html.Action("HtmlEditor3Partial", "Devexpress")*@
            }
        </div>
        <div class="col-md-3 pull-right" style="margin-top:20px">
            <br />
            <input type="submit" value="Cập nhật" class="btn btn-block btn-success"/>
        </div>
    </div>
}